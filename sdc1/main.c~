#include <stdio.h>
#include <stdlib.h>
#include <math.h>


#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

typedef struct {
    int x;
    int y;
    int w;
} Rect;

typedef struct {
    SDL_Texture *tex;
    Rect r;
} Sprite;

typedef struct {
    void *items;
    int length;
} Vect;
